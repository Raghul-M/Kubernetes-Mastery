#Generated Kubernetes YAML manifests without actually creating the resource
# kubectl run nginx --image=nginx --dry-run=client -o yaml >pod.yaml

apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
